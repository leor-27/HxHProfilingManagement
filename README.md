🚀 Features

You should first click Add User at the top right to begin entering information for a new user.
Once you click Add User, the text fields and buttons will appear.
The Image, Given Name, Gender, and NEN Type fields are required.
After filling in all the necessary information, click the Save/Edit Profile button at the bottom. The user’s information will then appear in the table.
Click Add User again if you want to add another user.
If you want to edit an existing user, simply select that user’s row in the table. The fields will appear again, and after making your changes, click Save/Edit Profile to update the information.

📌 Specifics

This project uses the MVC architectural pattern/framework .
The Controller package (where HomeController is located), the Model package (where User.java is located), and the HelloApplication.java can be found in: src/main/java/com/example/hxhprofilemanagement (com.example.hxhprofilemanagement as a package)
The images and the View package (where the home-view.fxml is stored) can be found in: src/main/resources/com/example/hxhprofilemanagement (com.example.hxhprofilemanagement as a package)